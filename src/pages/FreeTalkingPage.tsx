// AI 프리토킹 페이지

import styled from "styled-components";

const Layout = styled.div`
  position: relative;
  width: 100%;
  min-width: 1280px;
  height: 200vh;
`;

function FreeTalkingPage() {
  return <Layout></Layout>;
}

export default FreeTalkingPage;
